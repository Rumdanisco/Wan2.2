torch>=2.4.0
torchvision>=0.19.0

# Core diffusion + transformer stack
diffusers>=0.31.0
transformers>=4.49.0,<=4.51.3
tokenizers>=0.20.3
accelerate>=1.1.1

# Utility + numerical dependencies
tqdm
numpy>=1.23.5,<2
opencv-python>=4.9.0.80
Pillow
scipy

# Video and image IO
imageio[ffmpeg]
imageio-ffmpeg
decord

# Config and text helpers
easydict
ftfy
einops==0.8.0
huggingface-hub>=0.23.0
pyyaml

# RunPod integration
runpod==1.6.2

# ✅ GitHub source ensures latest compatible diffusers version (needed for Wan models)
git+https://github.com/huggingface/diffusers
